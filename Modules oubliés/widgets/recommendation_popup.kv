

<RecommendationPopup>:
    orientation: 'vertical'
    padding: dp(20)
    spacing: dp(15)
    size_hint: None, None
    size: dp(520), dp(300)
    pos_hint: {"center_x": 0.5, "top": 0.98}
    canvas.before:
        Color:
            rgba: 0.06, 0.08, 0.1, 0.95
        RoundedRectangle:
            pos: self.pos
            size: self.size
            radius: [18]

    Label:
        text: "💡 IA recommandées pour : '{}'".format(root.task_description)
        font_size: '16sp'
        size_hint_y: None
        height: dp(30)
        bold: True
        color: 0.8, 0.9, 1, 1

    BoxLayout:
        orientation: 'vertical'
        size_hint_y: None
        height: self.minimum_height
        spacing: dp(10)
        padding: dp(5), 0

        # Recommandations dynamiques
        Label:
            text: "\\n".join(["{} (≈ {:.3f} $)".format(m, estimate_cost(m, 800)) for m in root.recommended_models])
            font_size: '15sp'
            markup: True
            text_size: self.size
            valign: 'top'
            halign: 'left'

    Label:
        text: "💰 Coût total estimé : {:.3f} $".format(root.total_cost)
        font_size: '16sp'
        color: 0.7, 1, 0.7, 1
        size_hint_y: None
        height: dp(30)

    Button:
        text: "🔍 Ajuster la sélection IA"
        size_hint_y: None
        height: dp(40)
        font_size: '15sp'
        background_color: 0.3, 0.5, 1, 1
        on_release: app.open_ia_overlay()

    Button:
        text: "🚀 Lancer la tâche"
        size_hint_y: None
        height: dp(42)
        font_size: '16sp'
        bold: True
        background_color: 0.2, 0.7, 0.3, 1
        on_release: root.confirm_and_execute()

    Button:
        text: "📜 Voir l’historique"
        size_hint_y: None
        height: dp(40)
        font_size: '15sp'
        background_color: 0.2, 0.4, 0.8, 1
        on_release: root.toggle_history()

    IAHistoryWidget:
        id: history_widget
        opacity: 0
        height: 0
        canvas.before:
            Color:
                rgba: 0.05, 0.05, 0.1, 0.9
            RoundedRectangle:
                pos: self.pos
                size: self.size
                radius: [12]
